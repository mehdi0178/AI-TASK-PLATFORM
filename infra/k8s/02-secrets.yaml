# SECURITY NOTE: this file is a TEMPLATE checked into the infra repo with
# placeholder values only. Real values are injected at deploy time via
# `kubectl create secret` / a secrets manager (see docs/architecture.md).
# Do NOT put actual production secrets in git.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: ai-task-platform
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_STRONG_RANDOM_SECRET"
  MONGO_ROOT_USERNAME: "REPLACE_ME"
  MONGO_ROOT_PASSWORD: "REPLACE_ME"
